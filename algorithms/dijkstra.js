class Graph {
  constructor(row, cols) {
    this.rows = rows; // Number of rows and columns
    this.cols = cols; // Number of rows and columns
    this.grid = this.createGrid(); // Create a grid
  }
}
